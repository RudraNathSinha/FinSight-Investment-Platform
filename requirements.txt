fastapi>=0.110,<0.142
uvicorn[standard]>=0.27,<0.53
yfinance>=0.2.40
reportlab>=4.0
pandas>=2.0
httpx>=0.27
cachetools>=5.3
# Optional for richer PDF (Part 3 style); ignore if build fails on free tier
# weasyprint>=62.0
